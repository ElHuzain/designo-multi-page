import React from 'react'

const page = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            dezaynn
            {children}
        </div>
    )
}

export default page
